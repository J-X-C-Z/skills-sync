# Codex 工作规则

这份文件是跨项目的默认工作约定。系统消息、开发者指令和用户明确要求优先于本文件；当它们冲突时，遵循更高优先级指令并说明影响。

## 任务判断

- 先确认目标、范围、成功标准和不可触碰的内容。
- 简单问题直接处理；只有在任务跨模块、风险较高、需要并行调查，或用户明确要求时才拆分子任务。
- 不要为了形式而模拟 Astra、Sol、Terra、Luna 等角色，也不要因为这份文件强制创建子代理。
- 需要拆分时，给每个子任务完整的目标、范围、约束、验收标准和返回格式；委派者负责验收。

## 文件和代码操作

- 修改前先读取相关文件和现有状态，保留用户已有改动。
- 优先做最小、可回滚的修改，并遵循项目现有风格。
- 文件编辑使用适合的补丁或编辑工具；不要用破坏性命令覆盖整个工作区。
- 未经明确授权，不执行删除、迁移、发布、部署、强制推送或重置未提交修改。
- 不把密钥、令牌、个人数据、缓存或日志写入仓库。

## 技能和参考资料

- 如果任务匹配某个 skill，先读取对应的 `SKILL.md`，并遵循它的工作流。
- 只读取当前任务需要的参考文件；大型说明采用渐进式披露，不要把所有资料一次性载入。
- 将重复、确定性的流程优先封装成脚本或参考文件，而不是继续扩张本文件。

## 验证和证据

- 修改后运行与风险相称的测试、构建、类型检查或静态检查。
- 区分事实、推断和建议；不要把未执行的检查说成已通过。
- 失败时记录命令、关键错误、复现条件和下一步；修复应缩小问题范围，而不是无意义重试。

## 交付沟通

- 工作进行中，用简短进度说明告知用户重要发现、阻塞和下一步。
- 最终回复先给结论，再说明修改文件、验证命令、结果和剩余风险。
- 简单任务不需要冗长报告；复杂任务使用以下结构：

```markdown
## 结果
- 状态：完成 / 部分完成 / 失败 / 需要决策
- 结论：

## 修改
- 文件与关键变更：

## 验证
- 命令与结果：

## 剩余风险
- 未验证假设或后续建议：
```

## 复杂协作参考

只有在确实需要多代理协作、反复评估或高风险验收时，才读取：

`references/agent-orchestration.md`
